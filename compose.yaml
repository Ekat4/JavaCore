version: ''

services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=StyleFit'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5433:5432'
